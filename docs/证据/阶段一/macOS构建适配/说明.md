# macOS 构建适配证据

## 同步结果

- 执行位置：`/Users/xingranya/Downloads/GitHub-clone/openvela-workspace`
- 命令：`./.repo/repo/repo sync -c -j1 --fail-fast docs`
- 结果：成功
- `docs` HEAD：`388302cbb5f9b17a42008dc005c1f53fde4b65de`

## 工具版本

| 工具 | 版本 |
|:---|:---|
| Homebrew Bash | 5.3.15 |
| GNU sed | 4.10 |
| Xtensa GCC | 12.2.0 |
| Xtensa GDB | 12.1_20221002 |
| esptool | 4.8.dev4 |
| Ninja | 1.13.2 |
| 构建并发数 | 10（`sysctl -n hw.logicalcpu`） |

## 适配入口

| 文件 | 职责 |
|:---|:---|
| `scripts/macos环境.sh` | 注入 Python、GCC、GDB、GNU sed 与并发数 |
| `scripts/检查开发环境.sh` | 只读检查依赖和官方配置文件 |
| `scripts/构建固件.sh` | 统一板级基线与 ai_agent 官方构建流程 |
| `scripts/烧录固件.sh` | 自动发现 macOS 串口并烧录 `0x0` |
| `scripts/串口监视.sh` | 使用 `screen` 打开 115200 USB-CDC 串口 |

## 验证结果

- `/opt/homebrew/bin/bash -n scripts/*.sh`：通过。
- `./scripts/检查开发环境.sh`：全部检查项通过。
- `./scripts/构建固件.sh openvela --clean --dry-run`：官方绝对 board config 路径和 `distclean` 顺序正确。
- `./scripts/构建固件.sh ai_agent --clean --dry-run`：defconfig、`distclean`、后台修复脚本和构建顺序正确。
- 脚本中不存在 `nproc`、`/dev/ttyACM*` 或 `/dev/serial/by-id` 硬编码。

本任务未执行真实编译、烧录或串口启动；这些操作属于 T1.5。
