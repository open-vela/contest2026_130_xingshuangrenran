# repo-managed 参赛仓改动保护说明

## 快照范围

- **源工作区**：`/Users/xingranya/Downloads/GitHub-clone/openvela-workspace/contest2026_130_xingshuangrenran`
- **基线提交**：`1294cdf57561989536af6f7e11bccedf5aaf404a`
- **提交标题**：`Initial commit: scaffold contest repo for team xingshuangrenran`
- **工作树状态**：detached HEAD，15 个已跟踪文件修改，1 个未跟踪 `.DS_Store`
- **改动规模**：27 行增加、27 行删除
- **快照日期**：2026-07-12

## 审计结论

15 个源码与配置文件的改动均属于模板身份一致性修正：

- 将构建符号、映射目录、包名、界面文字和说明中的 `000` 替换为队伍编号 `130`。
- 未引入产品业务逻辑、板级真实实现或额外依赖。
- `git diff --check` 通过，无空白错误。
- 未跟踪 `.DS_Store` 是 6148 字节的 Finder 元数据，已记录哈希但不纳入源码补丁，原文件保持不动。

## 快照文件

- [Git状态快照.txt](./Git状态快照.txt)：HEAD、远端、文件状态和改动统计。
- [未提交改动.patch](./未提交改动.patch)：可由 Git 反向恢复的完整文本补丁。
- [文件校验值.sha256](./文件校验值.sha256)：15 个修改文件及 `.DS_Store` 的 SHA-256。

## 恢复验证

在与基线提交一致的临时检出中执行：

```bash
git apply --check docs/证据/阶段一/工作区改动保护/未提交改动.patch
git apply docs/证据/阶段一/工作区改动保护/未提交改动.patch
head -n 15 docs/证据/阶段一/工作区改动保护/文件校验值.sha256 \
  | shasum -a 256 -c -

# 仅在需要核对原工作区 Finder 元数据时执行。
rg '\.DS_Store$' docs/证据/阶段一/工作区改动保护/文件校验值.sha256 \
  | shasum -a 256 -c -
```

补丁只覆盖 15 个已跟踪文件，不会创建、删除或恢复 `.DS_Store`。
